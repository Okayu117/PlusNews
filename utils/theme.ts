import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    heading: `'Daruma Drop', sans-serif`,
    body: `'Daruma Drop', sans-serif`,
  },
})


console.log(theme)

export default theme
